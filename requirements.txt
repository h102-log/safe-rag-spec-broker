# Phase 0-rag-core dependencies only.
# 버전 핀은 재량 — pip이 설치 가능한 조합으로 해석하도록 미지정.

# API
fastapi
uvicorn[standard]
pydantic-settings
python-dotenv

# Ingest: 파싱 / 청킹 / 임베딩
docling
FlagEmbedding
langchain-text-splitters
tiktoken

# Vector DB
psycopg[binary]
pgvector

# Generate: LLM
anthropic

# Observability
langfuse

# Eval: RAGAS
ragas
langchain-anthropic
# ragas 0.4.3이 langchain_community.chat_models.vertexai를 최상단 import하는데
# 이 모듈은 community 0.4에서 제거됨 — 핀 없으면 import ragas 자체가 깨진다(2026-07-24 확인).
langchain-community<0.4

# Tests
pytest
httpx
